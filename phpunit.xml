<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="/work/include/vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap-unit.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnWarning="true"
         failOnNotice="true"
         failOnDeprecation="true"
         beStrictAboutOutputDuringTests="false">
	<php>
		<ini name="error_reporting" value="-1"/>
		<env name="CACTI_TEST_BOOTSTRAP" value="1"/>
	</php>
	<testsuites>
		<testsuite name="Unit">
			<directory>./tests/Security</directory>
			<directory>./tests/Unit</directory>
			<directory>./tests/Integration</directory>
		</testsuite>
	</testsuites>

	<!--
		Pest reads this PHPUnit-compatible configuration through Cacti's
		Composer-managed vendor tree.

		thold_functions.php is the only plugin source that loads without a
		running Cacti; every other file includes ./include/auth.php and
		dispatches on the request at top level. setup.php is the exception:
		its functions are safe to require directly (no top-level side
		effects), which is what tests/Integration/PluginLifecycleTest.php does.
	-->
	<source>
		<include>
			<file>thold_functions.php</file>
			<file>setup.php</file>

		</include>
	</source>
</phpunit>
