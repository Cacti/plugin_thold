<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="/work/include/vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap-unit.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnWarning="true"
         failOnNotice="true"
         failOnDeprecation="true"
         beStrictAboutOutputDuringTests="false">
	<php>
		<ini name="error_reporting" value="-1"/>
		<env name="CACTI_TEST_BOOTSTRAP" value="1"/>
	</php>
	<testsuites>
		<testsuite name="Unit">
			<directory>./tests/Security</directory>
			<directory>./tests/Unit</directory>
			<directory>./tests/Integration</directory>
		</testsuite>
	</testsuites>

	<!--
		Pest reads this PHPUnit-compatible configuration through Cacti's
		Composer-managed vendor tree.

		thold_functions.php, includes/polling.php, and setup.php are the only
		plugin sources that load without a running Cacti (no top-level side
		effects); every other file includes ./include/auth.php and dispatches
		on the request at top level. tests/Integration/PluginLifecycleTest.php
		is what requires setup.php directly.
	-->
	<source>
		<include>
			<file>thold_functions.php</file>
			<file>includes/polling.php</file>
			<file>setup.php</file>

		</include>
	</source>
</phpunit>
